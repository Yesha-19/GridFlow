"""recommendation.py — Resource recommendation API placeholder."""

from fastapi import APIRouter

router = APIRouter()
# Resource recommendations are served as part of the /api/predict response.
# This module exists for future standalone resource re-calculation endpoints.
